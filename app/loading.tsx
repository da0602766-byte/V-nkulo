export default function Loading() {
  return <main className="pilot-loading" aria-busy="true" aria-live="polite"><span className="pilot-loader" /><strong>Carregando o VÍNKULO…</strong><p>Validando sessão, comunidade ativa e permissões.</p></main>;
}
